# Global contract pointers

This is a delta implementation package. It does not duplicate the entire Claude Design upload bundle.

Before implementation, read these current authoritative files from the shared machine:

- `/Users/aziz.u/agent-context/snaplist-claude-design-handoff.md`
- `/Users/aziz.u/Developer/snaplist-claude-design-bundle-v6/snaplist-claude-design-packet/FULL-DESIGN-SCOPE.md`

If a local package file conflicts with a locked product decision in those sources, stop and reconcile the conflict rather than inventing behavior. The visual source controls appearance for the approved states; the global contract controls product truth and scope.

Do not use stale repo snapshots containing daily-capacity, launch-autopilot, or superseded paywall language.
