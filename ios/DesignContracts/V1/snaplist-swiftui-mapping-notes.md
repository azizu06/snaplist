# SwiftUI mapping stop notice

The former V1 mapping sequence is superseded by epic #349. This file contains no SwiftUI
implementation authorization.

The future approved package must map exactly two primary destinations, **Scan** and **Trophy Wall**,
with Settings opened from the profile avatar. It must preserve one-to-five photo intake, optional
fifteen-second voice context, asynchronous durable processing, plain-language progress, first value
before signup/paywall, direct eBay publish only, and honest export packs elsewhere.

Until the redirected high-fidelity package and owning issue are approved:

1. Do not implement the retired five-tab shell, Seller Home, Inbox, Insights, activity center,
   separate Runs, post-sale, bulk/haul, barcode-only, or measurement states.
2. Do not treat the retired screenshots, tokens, routes, copy, or assets as current acceptance
   references.
3. Keep SwiftUI behind provider-neutral server contracts. Do not duplicate RLS, App Attest, durable
   pipeline, credit, pricing, correction, or marketplace policy in the client.
4. Preserve Dynamic Type, VoiceOver, Reduced Motion, non-color status cues, truthful progress, and
   minimum 44-by-44-point targets in the future approved composition.
